extends layout

block content
  h1=title

  form(method='POST' action='')
    div.form-group
      label(for='first_name') Nombre:
      input#first_name.form-control(type='text' placeholder='nombre' name='first_name' required='true' value=(undefined===author ? '' : author.first_name) )
      label(for='family_name') Appellido:
      input#family_name.form-control(type='text' placeholder='apellido' name='family_name' required='true' value=(undefined===author ? '' : author.family_name))
    div.form-group
      label(for='date_of_birth') Fecha nacimiento:
      input#date_of_birth.form-control(type='date' name='date_of_birth' value=(undefined===author ? '' : author.date_of_birth_formatted) )
      label(for='date_of_death') Fecha de fallecimiento:
      input#date_of_death.form-control(type='date' name='date_of_death' value=(undefined===author ? '' : author.date_of_death_formatted ))
    button.btn.btn-primary(type='submit') Aceptar
  if errors 
    ul
      for error in errors
        li!= error.msg